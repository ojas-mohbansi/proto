# proto
Proto is a proactive agent, not a chat bot. It decomposes goals via HTN planning, consults LLM + memory for safe actions, executes in a sandbox, learns from outcomes, reports via dashboard/WhatsApp, auto‑recovers with watchdog, runs locally with Ollama or any OpenAI‑compatible endpoint.
